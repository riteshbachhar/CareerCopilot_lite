{
  "name": "careerpilot-lite",
  "version": "0.0.1",
  "private": true,
  "description": "Local-first job tracker Chrome extension with profile-grounded match scoring.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "clean": "rm -rf dist",
    "test": "node --test tests/*.test.js"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  },
  "dependencies": {
    "@huggingface/transformers": "^4.1.0",
    "@mozilla/readability": "^0.6.0"
  }
}
